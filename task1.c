#include <stdio.h>

int main() //encryption with rotation cipher given text and rotation amount
{
    int i;
    char c = i;
    for(i >= 65, i <= 90) {
        char letter[26];
    }
    printf("%c", 1);
}